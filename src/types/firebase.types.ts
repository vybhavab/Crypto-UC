export interface User {
  googleId: string;
  account_type: string;
  campus_id: string;
  cardano_wallet_id: string;
  cardano_acct_addr: string;
  email: string;
  imageUrl: string;
  name: string;
}
